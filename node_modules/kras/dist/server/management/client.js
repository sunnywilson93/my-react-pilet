"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.clientOf = void 0;
const helpers_1 = require("../helpers");
function clientOf(server, config) {
    const index = helpers_1.getClient(config.directory, config.client);
    const target = config.api + '/';
    return (req, res) => {
        if (req.url.endsWith('/')) {
            res.sendFile(index);
        }
        else {
            res.redirect(target);
        }
    };
}
exports.clientOf = clientOf;
