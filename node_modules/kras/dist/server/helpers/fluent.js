"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.runWith = exports.configureHandler = void 0;
function configureHandler(server, config) {
    const handlers = config && config.handlers;
    if (handlers) {
        for (const url of Object.keys(handlers)) {
            const handler = handlers[url];
            server.at(url).any(handler);
        }
    }
}
exports.configureHandler = configureHandler;
function runWith(server, callback) {
    return server
        .start()
        .then(() => callback(server))
        .then(() => server.stop());
}
exports.runWith = runWith;
