"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.createFileFromTemplateIfNotExists = exports.fillTemplate = exports.applyTemplate = void 0;
const ejs_1 = require("ejs");
const fs_1 = require("fs");
const path_1 = require("path");
const log_1 = require("./log");
const io_1 = require("./io");
function applyTemplate(file, data) {
    return __awaiter(this, void 0, void 0, function* () {
        const content = yield new Promise((resolve, reject) => {
            (0, log_1.log)('generalDebug_0003', `Filling template in "${file}".`);
            (0, ejs_1.renderFile)(file, data, (err, str) => {
                if (err) {
                    reject(err);
                }
                else {
                    resolve(str);
                }
            });
        });
        yield new Promise((resolve, reject) => {
            (0, fs_1.writeFile)(file, content, 'utf8', (err) => {
                if (err) {
                    reject(err);
                }
                else {
                    resolve();
                }
            });
        });
    });
}
exports.applyTemplate = applyTemplate;
function fillTemplate(name, data = {}) {
    const path = (0, path_1.resolve)(__dirname, '..', '..', 'templates', `${name}.ejs`);
    return new Promise((resolve, reject) => {
        (0, log_1.log)('generalDebug_0003', `Rendering template "${name}" in "${path}".`);
        (0, ejs_1.renderFile)(path, data, (err, str) => {
            if (err) {
                reject(err);
            }
            else {
                resolve(str);
            }
        });
    });
}
exports.fillTemplate = fillTemplate;
function createFileFromTemplateIfNotExists(prefix, targetDir, fileName, forceOverwrite, data) {
    return __awaiter(this, void 0, void 0, function* () {
        const content = yield fillTemplate(`${prefix}-${fileName}`, data);
        yield (0, io_1.createFileIfNotExists)(targetDir, fileName, content, forceOverwrite);
    });
}
exports.createFileFromTemplateIfNotExists = createFileFromTemplateIfNotExists;
//# sourceMappingURL=template.js.map