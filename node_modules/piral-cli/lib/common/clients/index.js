"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.detectClients = exports.isWrapperClient = exports.clients = void 0;
const lerna = require("./lerna");
const npm = require("./npm");
const pnpm = require("./pnpm");
const rush = require("./rush");
const yarn = require("./yarn");
exports.clients = {
    lerna,
    npm,
    pnpm,
    rush,
    yarn,
};
const directClients = ['npm', 'yarn', 'pnpm'];
function isWrapperClient(client) {
    return !directClients.includes(client);
}
exports.isWrapperClient = isWrapperClient;
function detectClients(root) {
    return Promise.all(Object.keys(exports.clients).map((client) => __awaiter(this, void 0, void 0, function* () {
        const result = yield exports.clients[client].detectClient(root);
        return {
            client,
            result,
        };
    })));
}
exports.detectClients = detectClients;
//# sourceMappingURL=index.js.map