"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.normalizePublicUrl = void 0;
function normalizePublicUrl(url) {
    if (!url) {
        return '/';
    }
    if (!url.endsWith('/')) {
        url = `${url}/`;
    }
    if (!url.startsWith('/')) {
        url = `/${url}`;
    }
    return url;
}
exports.normalizePublicUrl = normalizePublicUrl;
//# sourceMappingURL=url.js.map