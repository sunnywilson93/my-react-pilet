"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const common_1 = require("../common");
let handler;
function run(root, piral, emulator, standalone, sourceMaps, contentHash, minify, externals, publicUrl, outFile, outDir, entryFiles, logLevel, args) {
    (0, common_1.setStandardEnvs)({
        production: !emulator,
        root,
        publicPath: publicUrl,
        debugPiral: emulator,
        debugPilet: emulator || standalone,
        piral,
        dependencies: externals,
    });
    return handler.create({
        root,
        entryFiles,
        outFile,
        outDir,
        externals,
        emulator,
        sourceMaps,
        contentHash,
        minify,
        publicUrl,
        hmr: false,
        logLevel,
        watch: false,
        args,
    });
}
process.on('message', (msg) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        switch (msg.type) {
            case 'init':
                handler = require(msg.path);
                break;
            case 'start':
                const bundler = yield run(process.cwd(), msg.piral, msg.emulator, msg.standalone, msg.sourceMaps, msg.contentHash, msg.minify, msg.externals, msg.publicUrl, msg.outFile, msg.outDir, msg.entryFiles, msg.logLevel, msg);
                const result = yield bundler.bundle();
                if (result) {
                    process.send({
                        type: 'done',
                        outDir: result.outDir,
                        outFile: result.outFile,
                    });
                }
                break;
        }
    }
    catch (error) {
        process.send({
            type: 'fail',
            error: error === null || error === void 0 ? void 0 : error.message,
        });
    }
}));
//# sourceMappingURL=run-build-piral.js.map